import React from "react";
import FadeElement from "../../widgets/FadeElement";

const Hint = () => {
  return (
    <FadeElement cls="hint" delay={1}>
      Hover to zoom
    </FadeElement>
  );
};

export default Hint;
